# Phase 1.0 前置准备 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 Phase 1.0 前置准备——Excel 版本锁定、结构探测、Step 1 字段核对、全链路数据流盘点、Supabase 现状摸底,为 1.1-1.10 提供依据。

**Architecture:** 本阶段产出 5 份文档 + 1 个 Python 脚本 + Step 1 表单字段对齐改动。不涉及业务计算逻辑改动(那是 1.6/1.8/1.9 的事)。所有探测结果写入 `docs/superpowers/progress/` 下的 Markdown 报告,作为后续子阶段的输入。

**Tech Stack:** Python 3 + openpyxl(Excel 探测)、TypeScript + React(Step 1 表单)、Supabase Studio(SQL 现状检查)、Markdown(报告)。

**Spec reference:** `docs/superpowers/specs/2026-07-11-knowledge-base-data-import-design.md` rev 7,重点是 §5.5(数据流盘点)、§5.9.6(1.0 前置依赖)、§8.1(Excel 结构探测脚本)、§8.6(Excel 版本锁)、§8.7(进度文件)、§8.9(Supabase 现状检查)。

**Estimated:** 1.5 个工作日。

---

## File Structure

| 路径 | 类型 | 责任 |
|---|---|---|
| `docs/superpowers/progress/phase-1-progress.md` | Create | Phase 1 跨 Session 进度跟踪 |
| `docs/superpowers/specs/excel-version-lock.md` | Create | 所有 Excel 文件的 sha1 hash + 修改时间,锁定录入版本 |
| `scripts/excel-structure-probe.py` | Create | 读桌面所有 Excel,输出结构报告 + 跨表一致性检查 |
| `docs/superpowers/progress/excel-structure-report.md` | Create(脚本生成) | 每个 Excel 的 sheet/列名/行数/特殊字符/数据类型 + 跨表差异 |
| `docs/superpowers/progress/step1-field-diff.md` | Create | Step 1 表单字段 4 类差异分类 |
| `docs/superpowers/progress/data-flow-audit.md` | Create | Step 2->3/3->4/3->5/4->5 四条断点盘点 + Step 2 综合节能率 Excel 结构确认 |
| `src/steps/step1-basic-info/components/SubStep*.tsx` | Modify(按需) | 只改影响计算的字段(补选项/改字段名/补缺失字段),UI 文案 1.10 再改 |

---

## Task 1: 建进度文件 phase-1-progress.md

**Files:**
- Create: `docs/superpowers/progress/phase-1-progress.md`

- [ ] **Step 1: 创建进度文件**

写入以下内容(spec 附录 C 模板):

```markdown
# Phase 1 进度跟踪

> 每个 Session 开头先读本文件,结尾更新它。决策记录格式见 spec §8.7。

## 当前状态
- 进行中: 1.0
- 下一步: 1.1

## 子阶段状态
| 子阶段 | 状态 | 计划工作量 | 实际工作量 | 备注 |
|---|---|---|---|---|
| 1.0 | in_progress | 1.5 天 | - | 前置准备 |
| 1.1 | pending | 0.5 天 | - | 技术卡片字段核对 |
| 1.2 | pending | 0.5 天 | - | 规范标准库基础部分 |
| 1.3 | pending | 3 天 | - | 模块2 计算表录入 |
| 1.4 | pending | 2 天 | - | 设备表替换 + Schema 扩展 |
| 1.5 | pending | 0.5 天 | - | 碳排放因子录入 |
| 1.6 | pending | 4 天 | - | Step 2 综合节能率接通 |
| 1.7 | pending | 0.5 天 | - | Step 3 投资汇总验证 |
| 1.8 | pending | 4.5 天 | - | Step 4 能耗/碳排放接通 |
| 1.9 | pending | 4.5 天 | - | Step 5 数据流贯通 |
| 1.10 | pending | 2.5 天 | - | 全链路联调 + UI 微调 |

## 决策记录
- 2026-07-11: 固定投资含运维费(用户确认)
  - 理由: 业务上"固定投资"包含运维初始投入
  - 备选: 不含运维(被否,用户确认含)
  - 影响: 1.7 验证公式 / Step 5 财务计算

## 遇到的问题
- (待记录)

## 下次 Session 接力点
- 当前从 1.0 开始,先建进度文件 + Excel 版本锁 + 探测脚本
```

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/progress/phase-1-progress.md
git commit -m "docs(phase-1): init progress tracking file"
```

---

## Task 2: 建 Excel 版本锁

**Files:**
- Create: `docs/superpowers/specs/excel-version-lock.md`

- [ ] **Step 1: 生成所有 Excel 文件的 hash 清单**

Run:
```bash
find "/Users/plus0/Desktop/技术交底-20260701" -type f \( -name "*.xlsx" -o -name "*.docx" -o -name "*.pdf" \) -print0 | \
  xargs -0 shasum -a 1 | sort -k2 > /tmp/excel-hash-list.txt
```

Expected: `/tmp/excel-hash-list.txt` 包含 30 行左右,每行 `<sha1>  <路径>`。

- [ ] **Step 2: 生成修改时间清单**

Run:
```bash
find "/Users/plus0/Desktop/技术交底-20260701" -type f \( -name "*.xlsx" -o -name "*.docx" -o -name "*.pdf" \) -exec stat -f "%Sm %N" -t "%Y-%m-%d %H:%M:%S" {} \; | sort -k4 > /tmp/excel-mtime-list.txt
```

- [ ] **Step 3: 创建版本锁文件**

Create `docs/superpowers/specs/excel-version-lock.md`,内容格式:

```markdown
# Excel 版本锁

> Phase 1 录入期间,所有 Excel 文件以此清单的 sha1 hash 为准。
> 如有更新,走"变更流程":记录差异到本文件底部"变更记录"小节,有选择地同步到 TS。

## 锁定时间
2026-07-11(Phase 1.0 开工)

## 文件清单

### 模块0:三库全书
| 文件 | sha1 | 修改时间 | 大小 |
|---|---|---|---|
| 规范标准全名录.xlsx | <hash> | <mtime> | <size> |
| ...(从 /tmp/excel-hash-list.txt 和 /tmp/excel-mtime-list.txt 整理) |

### 模块1:建筑基本信息
| 文件 | sha1 | 修改时间 | 大小 |
|---|---|---|---|
| 医院建筑基本信息表.xlsx | <hash> | <mtime> | <size> |

### 模块2:节能方案筛选
...(同上格式)

### 模块3:机电系统投资概算
...(12 个设备表,逐个列出)

### 模块4:节能计算与数据分析
...(碳排放因子 2 个 + 计算 docx 2 个)

### 模块5:投资决策报告模板
...(报告模板 docx + 可视化 pdf)

## 变更记录
(录入期间如有 Excel 更新,在此记录:日期、文件、变更内容、是否同步到 TS)
```

把 `/tmp/excel-hash-list.txt` 和 `/tmp/excel-mtime-list.txt` 的内容按模块整理到表格里。

- [ ] **Step 4: 验证文件完整性**

Run:
```bash
grep -c "^|" docs/superpowers/specs/excel-version-lock.md
```

Expected: 数字 >= 30(每个文件一行,加表头行)。如果 < 30,说明有文件漏录,回头检查 `find` 命令。

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/specs/excel-version-lock.md
git commit -m "docs(phase-1): lock Excel file versions with sha1 hash"
```

---

## Task 3: 写 Excel 结构探测脚本

**Files:**
- Create: `scripts/excel-structure-probe.py`

- [ ] **Step 1: 写脚本骨架 + 单文件探测函数**

Create `scripts/excel-structure-probe.py`:

```python
#!/usr/bin/env python3
"""
Excel 结构探测脚本 - Phase 1.0 前置

对桌面所有 Excel 文件输出:
- sheet 名列表
- 每个 sheet 的列名(第 1 行)
- 每个 sheet 的前 3 行数据
- 每个 sheet 的总行数
- 特殊字符扫描(£/R/合并单元格标记等)
- 数据类型推断(数字/字符串/日期)

跨表一致性检查:
- 6 张计算表的"机电系统"字段值要一致
- 12 个设备表的"技术名称"和 techEntries 的 techName 要一致
- 碳排放因子表的"省份"和 energyPriceReferences 的 location 要一致
- 12 个设备表的"运维分类"枚举值要统一

输出: docs/superpowers/progress/excel-structure-report.md
"""

import hashlib
import json
import os
from pathlib import Path
from typing import Any

from openpyxl import load_workbook
from openpyxl.utils import get_column_letter

DESKTOP_ROOT = Path("/Users/plus0/Desktop/技术交底-20260701")
OUTPUT_FILE = Path("docs/superpowers/progress/excel-structure-report.md")

# 特殊字符扫描清单
SPECIAL_CHARS = ["£", "R", " ", "﻿"]


def probe_sheet(ws) -> dict[str, Any]:
    """探测单个 sheet 的结构。"""
    result: dict[str, Any] = {
        "sheet_name": ws.title,
        "max_row": ws.max_row,
        "max_col": ws.max_column,
        "headers": [],
        "first_3_rows": [],
        "special_chars_found": [],
        "merged_cells_count": len(ws.merged_cells.ranges),
        "column_types": {},
    }

    # 列名(第 1 行)
    for col in range(1, ws.max_column + 1):
        val = ws.cell(row=1, column=col).value
        result["headers"].append(str(val) if val is not None else "")

    # 前 3 行数据(第 2-4 行)
    for row in range(2, min(5, ws.max_row + 1)):
        row_data = []
        for col in range(1, ws.max_column + 1):
            val = ws.cell(row=row, column=col).value
            row_data.append(val)
        result["first_3_rows"].append(row_data)

    # 列数据类型推断(基于前 10 行数据)
    for col in range(1, ws.max_column + 1):
        types_seen = set()
        for row in range(2, min(12, ws.max_row + 1)):
            val = ws.cell(row=row, column=col).value
            if val is None:
                continue
            types_seen.add(type(val).__name__)
        result["column_types"][result["headers"][col - 1]] = list(types_seen)

    # 特殊字符扫描(扫整个 sheet,但只记前 5 个出现位置)
    special_hits: list[dict[str, Any]] = []
    for row in ws.iter_rows():
        for cell in row:
            if cell.value is None:
                continue
            s = str(cell.value)
            for ch in SPECIAL_CHARS:
                if ch in s:
                    special_hits.append({
                        "char": ch,
                        "cell": f"{get_column_letter(cell.column)}{cell.row}",
                        "value": s[:50],
                    })
                    if len(special_hits) >= 5:
                        break
            if len(special_hits) >= 5:
                break
        if len(special_hits) >= 5:
            break
    result["special_chars_found"] = special_hits

    return result


def probe_file(filepath: Path) -> dict[str, Any]:
    """探测单个 Excel 文件。"""
    wb = load_workbook(filepath, data_only=True, read_only=False)
    sheets = []
    for ws in wb.worksheets:
        sheets.append(probe_sheet(ws))
    wb.close()
    return {
        "file_path": str(filepath.relative_to(DESKTOP_ROOT)),
        "file_name": filepath.name,
        "sheets": sheets,
    }


def main() -> None:
    OUTPUT_FILE.parent.mkdir(parents=True, exist_ok=True)

    # 收集所有 xlsx 文件
    xlsx_files = sorted(DESKTOP_ROOT.rglob("*.xlsx"))
    print(f"Found {len(xlsx_files)} xlsx files")

    results = []
    for f in xlsx_files:
        print(f"Probing: {f.name}")
        try:
            results.append(probe_file(f))
        except Exception as e:
            print(f"  ERROR: {e}")
            results.append({
                "file_path": str(f.relative_to(DESKTOP_ROOT)),
                "file_name": f.name,
                "error": str(e),
            })

    # 暂时只 dump JSON,下一步写 Markdown 渲染
    with open("/tmp/excel-probe-results.json", "w", encoding="utf-8") as f:
        json.dump(results, f, ensure_ascii=False, indent=2, default=str)

    print(f"\nResults dumped to /tmp/excel-probe-results.json")
    print(f"Next: render to Markdown in Step 2")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 跑脚本验证能跑通**

Run:
```bash
python3 scripts/excel-structure-probe.py
```

Expected: 输出 `Found N xlsx files`(N >= 20),逐个打印 `Probing: <文件名>`,最后打印 `Results dumped to /tmp/excel-probe-results.json`。如果有 ERROR,记录下来但继续跑完。

- [ ] **Step 3: 检查 JSON 输出**

Run:
```bash
python3 -c "import json; d=json.load(open('/tmp/excel-probe-results.json')); print(f'Files: {len(d)}'); print(f'First file sheets: {[s[\"sheet_name\"] for s in d[0][\"sheets\"]]}')"
```

Expected: 打印文件数和第一个文件的 sheet 名列表。如果第一个文件出错,打印其 error 字段。

- [ ] **Step 4: Commit 骨架**

```bash
git add scripts/excel-structure-probe.py
git commit -m "feat(phase-1): excel structure probe script skeleton"
```

---

## Task 4: 扩展探测脚本 - 渲染 Markdown + 跨表一致性检查

**Files:**
- Modify: `scripts/excel-structure-probe.py`

- [ ] **Step 1: 加 Markdown 渲染函数**

在 `probe_file` 函数后、`main` 函数前,加以下函数:

```python
def render_markdown(results: list[dict[str, Any]]) -> str:
    """把探测结果渲染成 Markdown 报告。"""
    lines: list[str] = [
        "# Excel 结构探测报告",
        "",
        f"> 探测时间: 2026-07-11(Phase 1.0)",
        f"> 文件总数: {len(results)}",
        "",
        "## 目录",
        "",
    ]
    # 目录
    for i, r in enumerate(results, 1):
        anchor = r["file_name"].replace(" ", "-").replace("(", "").replace(")", "").replace("（", "").replace("）", "")
        lines.append(f"{i}. [{r['file_name']}](#{i}-{anchor.lower()})")
    lines.append("")

    # 每个文件的详细结构
    for i, r in enumerate(results, 1):
        lines.append(f"## {i}. {r['file_name']}")
        lines.append("")
        lines.append(f"**路径**: `{r.get('file_path', '')}`")
        if "error" in r:
            lines.append(f"**ERROR**: {r['error']}")
            lines.append("")
            continue
        lines.append(f"**Sheet 数**: {len(r['sheets'])}")
        lines.append("")

        for sheet in r["sheets"]:
            lines.append(f"### Sheet: `{sheet['sheet_name']}`")
            lines.append("")
            lines.append(f"- 行数: {sheet['max_row']}")
            lines.append(f"- 列数: {sheet['max_col']}")
            lines.append(f"- 合并单元格数: {sheet['merged_cells_count']}")
            lines.append("")

            # 列名
            lines.append("**列名**:")
            lines.append("")
            for j, h in enumerate(sheet["headers"], 1):
                lines.append(f"{j}. `{h}`")
            lines.append("")

            # 前 3 行数据
            lines.append("**前 3 行数据**:")
            lines.append("")
            for row_idx, row in enumerate(sheet["first_3_rows"], 2):
                cells = [str(c)[:30] if c is not None else "" for c in row]
                lines.append(f"- 行 {row}: {' | '.join(cells)}")
            lines.append("")

            # 列数据类型
            lines.append("**列数据类型**:")
            lines.append("")
            for col_name, types in sheet["column_types"].items():
                lines.append(f"- `{col_name}`: {', '.join(types)}")
            lines.append("")

            # 特殊字符
            if sheet["special_chars_found"]:
                lines.append("**特殊字符扫描**:")
                lines.append("")
                for hit in sheet["special_chars_found"]:
                    lines.append(f"- `{hit['char']}` @ {hit['cell']}: {hit['value']}")
                lines.append("")

        lines.append("---")
        lines.append("")

    return "\n".join(lines)
```

- [ ] **Step 2: 改 main 函数,加 Markdown 输出**

把 `main` 函数末尾的 `print` 之前,加:

```python
    # 渲染 Markdown
    md = render_markdown(results)
    with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
        f.write(md)
    print(f"\nMarkdown report written to {OUTPUT_FILE}")
```

删掉原来的 `/tmp/excel-probe-results.json` dump(或保留作为 debug,加注释)。

- [ ] **Step 3: 加跨表一致性检查函数**

在 `render_markdown` 后,加:

```python
def check_cross_table_consistency(results: list[dict[str, Any]]) -> str:
    """跨表一致性检查,返回 Markdown 段落。"""
    lines: list[str] = [
        "## 跨表一致性检查",
        "",
    ]

    # 1. 6 张计算表的"机电系统"字段值一致性
    calc_tables = [
        "机电系统能耗权重表.xlsx",
        "医院整体修正系数表.xlsx",
        "技术组合重叠修正系数表.xlsx",
    ]
    system_values: dict[str, set[str]] = {}
    for r in results:
        if r["file_name"] in calc_tables:
            for sheet in r.get("sheets", []):
                # 找"机电系统"列
                for j, h in enumerate(sheet["headers"]):
                    if "机电系统" in str(h) or "系统类型" in str(h):
                        vals = set()
                        for row in sheet["first_3_rows"]:
                            if j < len(row) and row[j] is not None:
                                vals.add(str(row[j]))
                        system_values.setdefault(r["file_name"], set()).update(vals)
    lines.append("### 1. 计算表的机电系统字段值")
    lines.append("")
    for fname, vals in system_values.items():
        lines.append(f"- **{fname}**: {', '.join(sorted(vals)) if vals else '(无)'}")
    if not system_values:
        lines.append("- (未找到机电系统列,需人工检查列名)")
    lines.append("")

    # 2. 12 个设备表的"技术名称"一致性
    tech_names: list[str] = []
    for r in results:
        if "模块3" in r.get("file_path", "") and r["file_name"].endswith(".xlsx"):
            tech_names.append(r["file_name"])
    lines.append("### 2. 模块3 设备表文件清单")
    lines.append("")
    for n in tech_names:
        lines.append(f"- {n}")
    lines.append(f"**总数**: {len(tech_names)}(应为 12)")
    lines.append("")

    # 3. 碳排放因子表的"省份"字段
    carbon_files = ["电力平均碳排放因子（省级电网）.xlsx", "化石能源碳排放因子.xlsx"]
    lines.append("### 3. 碳排放因子表的省份字段")
    lines.append("")
    for r in results:
        if r["file_name"] in carbon_files:
            for sheet in r.get("sheets", []):
                for j, h in enumerate(sheet["headers"]):
                    if "省" in str(h) or "地区" in str(h):
                        vals = set()
                        for row in sheet["first_3_rows"]:
                            if j < len(row) and row[j] is not None:
                                vals.add(str(row[j]))
                        lines.append(f"- **{r['file_name']}** 列 `{h}`: {', '.join(sorted(vals)) if vals else '(无)'}")
    lines.append("")

    # 4. 设备表"运维分类"枚举值
    lines.append("### 4. 设备表运维分类枚举值")
    lines.append("")
    maintenance_categories: set[str] = set()
    for r in results:
        if "模块3" in r.get("file_path", ""):
            for sheet in r.get("sheets", []):
                if "运维" not in sheet["sheet_name"] and "维护" not in sheet["sheet_name"]:
                    continue
                for j, h in enumerate(sheet["headers"]):
                    if "分类" in str(h) or "维护分类" in str(h):
                        for row in sheet["first_3_rows"]:
                            if j < len(row) and row[j] is not None:
                                maintenance_categories.add(str(row[j]))
    lines.append(f"枚举值汇总: {', '.join(sorted(maintenance_categories)) if maintenance_categories else '(未找到)'}")
    lines.append("")
    lines.append(f"**期望值**: `维保费用`, `运维人工费用`(spec §5.1)")
    lines.append("")

    return "\n".join(lines)
```

- [ ] **Step 4: 在 main 里调用跨表检查,追加到 Markdown 末尾**

改 main 函数,在 `md = render_markdown(results)` 后加:

```python
    # 跨表一致性检查
    cross_check = check_cross_table_consistency(results)
    md = md + "\n" + cross_check
    with open(OUTPUT_FILE, "w", encoding="utf-8") as f:
        f.write(md)
```

(原来的 `with open(OUTPUT_FILE, "w"...) as f: f.write(md)` 替换成上面这段)

- [ ] **Step 5: 跑脚本验证**

Run:
```bash
python3 scripts/excel-structure-probe.py
```

Expected: 末尾打印 `Markdown report written to docs/superpowers/progress/excel-structure-report.md`。

- [ ] **Step 6: 检查报告内容**

Run:
```bash
wc -l docs/superpowers/progress/excel-structure-report.md
```

Expected: 行数 >= 200(20+ 文件,每个文件至少 10 行)。

Run:
```bash
head -50 docs/superpowers/progress/excel-structure-report.md
```

Expected: 看到目录 + 第一个文件的 sheet 结构。如果某个文件 ERROR,记录到 `phase-1-progress.md` 的"遇到的问题"小节。

- [ ] **Step 7: Commit**

```bash
git add scripts/excel-structure-probe.py docs/superpowers/progress/excel-structure-report.md
git commit -m "feat(phase-1): excel structure probe with markdown report + cross-table check"
```

---

## Task 5: Step 1 字段核对 - 读 Excel

**Files:**
- Read: `/Users/plus0/Desktop/技术交底-20260701/模块1：建筑基本信息/医院建筑基本信息表.xlsx`

- [ ] **Step 1: 写一次性脚本读 Excel 字段**

Run:
```bash
python3 -c "
from openpyxl import load_workbook
wb = load_workbook('/Users/plus0/Desktop/技术交底-20260701/模块1：建筑基本信息/医院建筑基本信息表.xlsx', data_only=True)
for ws in wb.worksheets:
    print(f'=== Sheet: {ws.title} (rows={ws.max_row}, cols={ws.max_column}) ===')
    for row in ws.iter_rows(min_row=1, max_row=min(ws.max_row, 60), values_only=False):
        cells = [(c.value, c.coordinate) for c in row if c.value is not None]
        if cells:
            print(cells)
    print()
wb.close()
" > /tmp/step1-excel-fields.txt
wc -l /tmp/step1-excel-fields.txt
```

Expected: 输出到 `/tmp/step1-excel-fields.txt`,行数取决于表内容。

- [ ] **Step 2: 读输出,整理字段清单**

Read `/tmp/step1-excel-fields.txt`,在笔记里整理出:
- 每个 sheet 的字段名(列名或行标题)
- 每个字段的选项值(如果有下拉选项或固定值)
- 字段类型(文本/数字/日期/选项)

这是后续和代码对比的基准,先不写报告,等 Task 7 读代码后一起写差异报告。

---

## Task 6: Step 1 字段核对 - 读代码

**Files:**
- Read: `src/steps/step1-basic-info/components/SubStep1Author.tsx`
- Read: `src/steps/step1-basic-info/components/SubStep2Client.tsx`
- Read: `src/steps/step1-basic-info/components/SubStep3Hospital.tsx`
- Read: `src/steps/step1-basic-info/components/SubStep4MEP.tsx`
- Read: `src/steps/step1-basic-info/components/SubStep5Policy.tsx`
- Read: `src/shared/stores/projectStore.ts`(找 `Step1Data` interface 定义)

- [ ] **Step 1: 读 5 个 SubStep 组件,提取字段名 + 选项值**

逐个 Read 文件,在笔记里整理出每个 SubStep 的:
- 表单字段名(Form.Item 的 name)
- 字段类型(Input/Select/Radio/Checkbox/number)
- 选项值(Select/Radio 的 options)
- 对应的 store 字段(Step1Data interface 里的字段)

- [ ] **Step 2: 读 Step1Data interface 定义**

Run:
```bash
grep -n "interface Step1Data" -A 80 src/shared/stores/projectStore.ts
```

Expected: 看到 Step1Data interface 的完整定义,包含所有字段名和类型。

- [ ] **Step 3: 整理代码侧字段清单**

在笔记里整理出:
- Step1Data interface 的所有字段(分组:SubStep1Author/SubStep2Client/SubStep3Hospital/SubStep4MEP/SubStep5Policy)
- 每个字段的类型
- 每个字段的选项值(从 SubStep 组件代码里抠出来)

---

## Task 7: Step 1 字段核对 - 生成差异报告

**Files:**
- Create: `docs/superpowers/progress/step1-field-diff.md`

- [ ] **Step 1: 创建差异报告,4 类差异分类**

Create `docs/superpowers/progress/step1-field-diff.md`,结构:

```markdown
# Step 1 表单字段差异报告

> 对比基准: `/Users/plus0/Desktop/技术交底-20260701/模块1：建筑基本信息/医院建筑基本信息表.xlsx`
> 代码位置: `src/steps/step1-basic-info/components/SubStep*.tsx` + `src/shared/stores/projectStore.ts` 的 `Step1Data`
> 生成时间: 2026-07-11(Phase 1.0)

## 影响计算字段清单

以下字段的差异会影响 Step 2/3/4/5 计算,必须当场改代码对齐 Excel(spec §2.3):

| 字段 | 用途 | 影响哪个 Step |
|---|---|---|
| hospitalType | 医院类型,查能耗限额/医院整体修正 | Step 2/4 |
| buildingType | 建筑类型 | Step 2/4 |
| hospitalScale | 医院规模 | Step 2/4 |
| hospitalLevel | 医院等级 | Step 2/4 |
| normalBeds/icuBeds/operatingRooms | 床位/手术室数 | Step 2/4 |
| totalArea/aboveGroundArea/cleanArea | 面积 | Step 2/4 |
| location | 所在地,查碳排放因子/能源价格 | Step 4 |
| mep.* | 机电系统选择,查能耗权重 | Step 4 |
| energy.peakValleyDiff/valleyHours | 峰谷差/谷电时长 | Step 4 |

## 1. 字段名不一致

(Excel 叫"医院类型",代码叫 `hospitalType` -- 字段名差异)

| Excel 字段名 | 代码字段名 | SubStep | 处理 |
|---|---|---|---|
| ...(逐个列出) | | | |

## 2. 选项值不一致

(同一字段,Excel 选项和代码 options 不同)

| 字段 | Excel 选项 | 代码选项 | SubStep | 处理 |
|---|---|---|---|---|
| ...(逐个列出) | | | | |

## 3. 代码缺字段(Excel 有,代码没有)

| Excel 字段 | 用途 | SubStep | 是否影响计算 | 处理 |
|---|---|---|---|---|
| ...(逐个列出) | | | | |

## 4. Excel 缺字段(代码有,Excel 没有)

| 代码字段 | 用途 | SubStep | 处理 |
|---|---|---|---|
| ...(逐个列出) | | | |

## 影响计算字段对齐计划

(Task 8 要改的字段清单,按 SubStep 分组)
```

- [ ] **Step 2: 填写差异报告**

基于 Task 5 的 Excel 字段清单 + Task 6 的代码字段清单,逐项对比填表。重点先填"影响计算字段清单"和 4 类差异表。

如果某类差异为空,写"无差异"。

- [ ] **Step 3: 验证报告完整性**

Run:
```bash
grep -c "^|" docs/superpowers/progress/step1-field-diff.md
```

Expected: >= 10(至少有表头和几行数据)。如果是 0,说明报告没填,回头补。

- [ ] **Step 4: Commit(差异报告,改动在 Task 8 单独 commit)**

```bash
git add docs/superpowers/progress/step1-field-diff.md
git commit -m "docs(phase-1): step1 field diff report (excel vs code)"
```

---

## Task 8: Step 1 影响计算字段对齐

**Files:**
- Modify: `src/steps/step1-basic-info/components/SubStep*.tsx`(按差异报告)
- Modify: `src/shared/stores/projectStore.ts`(如需改 Step1Data interface)
- Modify: `src/data/seedProject.ts`(如需同步 seed 默认值)

- [ ] **Step 1: 按 step1-field-diff.md 改影响计算的字段**

根据 Task 7 的差异报告"影响计算字段对齐计划"小节,逐项改:
- 补选项:在 Select/Radio 的 options 数组里加 Excel 选项
- 改字段名:Form.Item 的 name 改成和 Excel 对齐(同时改 store 字段名 + seed 字段名)
- 补缺失字段:Excel 有代码没有的影响计算字段,加 Form.Item + store 字段 + seed 默认值

**不改**不影响计算的字段(UI 文案、显示顺序等),记录到 `step1-field-diff.md` 的"1.10 待处理"小节。

- [ ] **Step 2: 同步改 Step1Data interface**

如果改了字段名或加了字段,`src/shared/stores/projectStore.ts` 的 `Step1Data` interface 要同步改。

- [ ] **Step 3: 同步改 seedProject.ts 的 getSeedStep1Data**

如果改了字段名或加了字段,`src/data/seedProject.ts:64-187` 的 `getSeedStep1Data()` 要同步改默认值。

- [ ] **Step 4: 跑 lint + build 验证**

Run:
```bash
npm run lint 2>&1 | tail -20
```

Expected: 无 error(warning 可接受)。如果有 error(如类型不匹配),修到通过。

Run:
```bash
npm run build 2>&1 | tail -20
```

Expected: build 成功。如果失败,看错误信息修(通常是 TypeScript 类型不匹配)。

- [ ] **Step 5: 跑 dev server 人工验证**

Run:
```bash
npm run dev
```

打开浏览器,登录,进入 Step 1,逐个子步骤检查:
- 表单能正常显示
- 选项能正常选择
- 保存后数据能持久化(刷新页面数据还在)
- seed 项目的默认值显示正确

如果发现 bug,修到能用。验证完关掉 dev server。

- [ ] **Step 6: Commit**

```bash
git add src/steps/step1-basic-info/components/ src/shared/stores/projectStore.ts src/data/seedProject.ts
git commit -m "feat(step1): align form fields with excel for calculation impact"
```

---

## Task 9: 数据流盘点 - Step 2 -> Step 3

**Files:**
- Read: `src/steps/step3-twins/index.tsx`
- Read: `src/steps/step3-twins/hooks/*.ts`(如有)
- Read: `src/shared/stores/projectStore.ts`(找 `projectsStep3SelectedTechs`)

- [ ] **Step 1: 读 Step 3 入口,找读取选中技术列表的代码**

Run:
```bash
grep -n "step2SelectedTechs\|projectsStep3SelectedTechs\|selectedTechs" src/steps/step3-twins/index.tsx src/steps/step3-twins/hooks/*.ts 2>/dev/null
```

记录:Step 3 是从哪个 store 字段读"选中的技术列表"的。

- [ ] **Step 2: 确认 techId 传递路径**

确认 Step 2 选中的 techId(如 `['1', '3', '4']`)能传到 Step 3。如果 Step 3 读的是 `projectsStep3SelectedTechs`(独立的字段),而非 `projectsStep2Data`(Step 2 的选择),记录这个差异。

- [ ] **Step 3: 写到 data-flow-audit.md 草稿**

Create `docs/superpowers/progress/data-flow-audit.md`(先建文件,后面 Task 会追加):

```markdown
# 数据流盘点报告

> Phase 1.0 前置,spec §5.5 要求。盘点 Step 2->3/3->4/3->5/4->5 四条断点。
> 1.6/1.8/1.9 开工前必读,确认数据传递字段和断点。

## 1. Step 2 -> Step 3

**传递字段**: techId 列表(选中的技术)

**当前状态**: 已通(seed 里 `projectsStep3SelectedTechs: { [SEED_PROJECT_ID]: ['1', '3', '4'] }`)

**盘点结果**:
- Step 3 读字段: `<填实际字段名,从 Task 9 Step 1 得到>`
- 传递路径: `<填,如 Step 2 写 projectsStep2Data -> Step 3 读 projectsStep3SelectedTechs>`
- 一致性: `<填,如 techId 一致 / 不一致>`

**1.1 待核对**: `techEntries` 的 techId 和 `techDefaultInvestments` 的 techId 完全一致(spec §2.3 1.1 范围)
```

把 `<填...>` 占位符替换成实际盘点结果。

- [ ] **Step 4: Commit(草稿,后面 Task 会追加内容)**

```bash
git add docs/superpowers/progress/data-flow-audit.md
git commit -m "docs(phase-1): data flow audit - step2 to step3"
```

---

## Task 10: 数据流盘点 - Step 3 -> Step 4

**Files:**
- Read: `src/steps/step4-energy/components/StepCalculation.tsx`
- Read: `src/steps/step4-energy/components/helpers.ts`(已读,重点看用了哪些 Step 3 字段)
- Read: `src/steps/step4-energy/components/DataAnalysis.tsx`(如有)
- Read: `src/shared/stores/projectStore.ts`(找 `Step4ProjectData.savingEquipments`/`originalEquipments`)

- [ ] **Step 1: 找所有读 step3Data 的代码**

Run:
```bash
grep -rn "step3Data\|techInvestment\|projectsStep3Data\|powerKw\|systemCapacity\|maintenanceCategory" src/steps/step4-energy/ | head -40
```

记录所有读取 Step 3 数据的代码位置和读取的字段名。

- [ ] **Step 2: 盘点 savingEquipments / originalEquipments 数据来源**

Run:
```bash
grep -n "savingEquipments\|originalEquipments" src/steps/step4-energy/ -r
grep -n "savingEquipments\|originalEquipments" src/shared/stores/projectStore.ts
```

确认:
- `Step4ProjectData.savingEquipments` 是否从 Step 3 的 `equipment` 表读?
- `Step4ProjectData.originalEquipments` 是否从 Step 3 读?
- 如果是,读哪些字段?如果不是,数据从哪来?

- [ ] **Step 3: 确认 Step 3 编辑后能传到 Step 4**

确认 Step 3 编辑视图保存后,数据写入 store,Step 4 能读到最新值。如果中间有缓存或转换,记录转换逻辑。

- [ ] **Step 4: 追加到 data-flow-audit.md**

在 `data-flow-audit.md` 末尾追加:

```markdown

## 2. Step 3 -> Step 4

**传递字段**: 设备功率(`powerKw`)/系统容量(`systemCapacity`)/运维分类(`maintenanceCategory`)/savingEquipments/originalEquipments

**当前状态**: 部分通(powerKw 已用),部分未盘(spec §5.5 要求 1.0 盘完)

**盘点结果**:
- Step 4 读字段: `<填,从 Task 10 Step 1 得到>`
- savingEquipments 来源: `<填,从 Step 3 equipment 表读 / 用户填 / 其他>`
- originalEquipments 来源: `<填>`
- 数据类型校验: `<填,字段在 TechInvestment/InvestmentRow 类型里存在 / 缺失>`

**1.4 schema 扩展依据**: `<填,InvestmentRow 要加哪些字段才支持 Step 4 读取>`
**1.8 接通依据**: `<填,Step 4 计算逻辑要改哪些>`
```

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/progress/data-flow-audit.md
git commit -m "docs(phase-1): data flow audit - step3 to step4"
```

---

## Task 11: 数据流盘点 - Step 3 -> Step 5

**Files:**
- Read: `src/steps/step5-decision/utils/financialCalculate.ts`
- Read: `src/steps/step5-decision/index.tsx`

- [ ] **Step 1: 读 financialCalculate.ts,找读 Step 3 数据的代码**

Run:
```bash
grep -n "step3\|projectsStep3Data\|techInvestment\|maintenance\|repairCost\|laborCost\|maintenanceCost" src/steps/step5-decision/utils/financialCalculate.ts
```

记录:当前 Step 5 是否读 Step 3 数据?读哪些字段?

(spec §5.4 说目前 Step 5 不读 Step 3,运维费用户重填。确认这个结论是否仍然成立。)

- [ ] **Step 2: 确认 maintenanceCategory 拆分逻辑接入点**

确认 `financialCalculate.ts` 里哪里要加 `deriveStep5MaintenanceFromStep3` 函数(spec §5.4)。

找:
- `repairCost`/`laborCost` 字段在哪里被读取(line 号)
- 这些字段的当前来源(用户填 / store 默认值 / 其他)

- [ ] **Step 3: 追加到 data-flow-audit.md**

```markdown

## 3. Step 3 -> Step 5

**传递字段**: maintenanceCost(按 maintenanceCategory 拆分 repairCost/laborCost)

**当前状态**: 不通(Step 5 重填,spec §2.2/§5.4)

**盘点结果**:
- Step 5 当前读字段: `<填,如 data.repairCost / data.laborCost,line 号>`
- 当前来源: `<填,如用户填 / seed 默认 0>`
- derive 函数接入点: `<填,如 financialCalculate.ts line XXX>`

**1.9 接通计划**:
- 加 `deriveStep5MaintenanceFromStep3`(spec §5.4)
- store 加载 Step 5 数据时调用 derive
- UI 改"自动填充 + 可编辑",dirty flag(spec §5.4)
```

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/progress/data-flow-audit.md
git commit -m "docs(phase-1): data flow audit - step3 to step5"
```

---

## Task 12: 数据流盘点 - Step 4 -> Step 5

**Files:**
- Read: `src/steps/step5-decision/utils/financialCalculate.ts`(已在 Task 11 读过,重点看读 Step 4 的部分)

- [ ] **Step 1: 找所有读 step4Data 的代码**

Run:
```bash
grep -n "step4\|projectsStep4Data\|savingCostRun\|originalCostRun\|annualEnergySaving\|energyCost\|custodialOperationFee\|totalFixedInvestment" src/steps/step5-decision/utils/financialCalculate.ts
```

记录:
- `savingCostRun`/`originalCostRun` 是否被读?(spec §5.6 说目前不读)
- `annualEnergySaving` 是否被读?(spec §5.6 说有字段但不读)
- `energyCost` 当前来源
- `custodialOperationFee` 当前来源(spec §5.6 说作为收入,line 380)
- `totalFixedInvestment` 当前来源 + fallback 路径(spec §5.8)

- [ ] **Step 2: 确认收入模型当前实现**

确认 `revenueExTax` 的计算逻辑(line 380 附近),验证 spec §5.6.1 的结论:收入 = custodialOperationFee / (1 + VAT)。

- [ ] **Step 3: 追加到 data-flow-audit.md**

```markdown

## 4. Step 4 -> Step 5

**传递字段**:
- `savingCostRun`(节能金额 = annualEnergySaving)
- `originalCostRun`(原能源费,算 energyCost)

**当前状态**: 不通(Step 5 重填,spec §5.6)

**盘点结果**:
- savingCostRun 是否被读: `<填,是 / 否>`
- originalCostRun 是否被读: `<填,是 / 否>`
- annualEnergySaving 是否被读: `<填,是 / 否>(spec §5.6 说有字段但不读,验证)`
- energyCost 当前来源: `<填,用户填 / seed / Step 4>`
- custodialOperationFee 当前用途: `<填,作为收入 / 其他>`
- totalFixedInvestment 当前来源 + fallback: `<填,如 data.totalFixedInvestment > 0 ? 用它 : initialInvestment + installationCost(缺 maintenanceCost,spec §5.8)>`

**1.9 接通计划**:
- 加 `deriveStep5EnergyCostFromStep4`(spec §5.6.3)
- 加 `deriveStep5IncomeFromStep4`(spec §5.6.4)
- 改 `revenueExTax` 按 investmentMode 分支(spec §5.6.1)
- 改 `calcProfit` 按年变化收入(spec §5.6.2)
- fallback 路径加 maintenanceCost(spec §5.8)
- UI 加 dirty flag(spec §5.6.6)
```

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/progress/data-flow-audit.md
git commit -m "docs(phase-1): data flow audit - step4 to step5"
```

---

## Task 13: Step 2 综合节能率 Excel 结构确认(spec §5.9.6)

**Files:**
- Read: `/Users/plus0/Desktop/技术交底-20260701/模块2：节能方案筛选/技术组合重叠修正系数表.xlsx`
- Read: `/Users/plus0/Desktop/技术交底-20260701/模块2：节能方案筛选/医院整体修正系数表.xlsx`
- Read: `/Users/plus0/Desktop/技术交底-20260701/模块2：节能方案筛选/机电系统能耗权重表.xlsx`
- Read: `/Users/plus0/Desktop/技术交底-20260701/模块2：节能方案筛选/医院建筑能耗限额标准汇总表.xlsx`

- [ ] **Step 1: 读 4 个 Excel 的结构**

Run:
```bash
python3 -c "
from openpyxl import load_workbook
files = [
    '技术组合重叠修正系数表.xlsx',
    '医院整体修正系数表.xlsx',
    '机电系统能耗权重表.xlsx',
    '医院建筑能耗限额标准汇总表.xlsx',
]
for fname in files:
    path = f'/Users/plus0/Desktop/技术交底-20260701/模块2：节能方案筛选/{fname}'
    wb = load_workbook(path, data_only=True)
    print(f'=== {fname} ===')
    for ws in wb.worksheets:
        print(f'  Sheet: {ws.title} (rows={ws.max_row}, cols={ws.max_column})')
        # 打印前 5 行
        for row in ws.iter_rows(min_row=1, max_row=min(5, ws.max_row), values_only=True):
            print(f'    {row}')
    wb.close()
    print()
"
```

记录每个表的:
- sheet 名
- 列名(或行标题,因为有些表是二维矩阵)
- 前 5 行数据样例
- 表结构(一维表 / 二维矩阵 / 混合)

- [ ] **Step 2: 确认 4 个问题(spec §5.9.6)**

基于 Step 1 的输出,确认:

1. **重叠修正系数表结构**:二维表(技术 A × 技术 B = 系数)?一维表?字段名?
2. **医院整体修正系数表查询维度**:按"医院类型"查?类型 × 规模?
3. **能耗权重表查询维度**:按"机电系统类型"查?技术 ID?
4. **能耗限额标准汇总表字段**:医院类型?单位面积能耗?哪些能源类型?

- [ ] **Step 3: 追加到 data-flow-audit.md**

```markdown

## 5. Step 2 综合节能率 Excel 结构确认(spec §5.9.6)

**1.6 开工前必读,1.0 定稿算法**

### 5.1 重叠修正系数表结构
- 文件: `技术组合重叠修正系数表.xlsx`
- 结构: `<填,二维矩阵 / 一维表 / 混合>`
- 字段名: `<填,如 techIdA / techIdB / correction>`
- 查询方式: `<填,两两查 / 按组合查 / 单一系数>`
- 1.6 算法选择: `<填,方案 A 两两求积 / 方案 B 三阶表 / 方案 C 平均系数>`

### 5.2 医院整体修正系数表结构
- 文件: `医院整体修正系数表.xlsx`
- 查询维度: `<填,医院类型 / 类型×规模 / 其他>`
- 字段名: `<填>`
- 1.6 查表函数签名: `getHospitalCorrection(hospitalType: string): number`

### 5.3 能耗权重表结构
- 文件: `机电系统能耗权重表.xlsx`
- 查询维度: `<填,机电系统类型 / 技术 ID / 其他>`
- 字段名: `<填>`
- 1.6 查表函数签名: `getEnergyWeight(systemType: string): number`

### 5.4 能耗限额标准汇总表结构
- 文件: `医院建筑能耗限额标准汇总表.xlsx`
- 字段名: `<填>`
- 查询方式: `<填,按医院类型查单位面积能耗 / 其他>`
- 1.6 用途: ComprehensiveRateModal 的 original 输入来源(spec §5.9.4)
  - `original.electricity = energyQuota[hospitalType].electricityPerSqm × totalArea`

### 5.5 1.6 算法定稿

基于以上探测,1.6 综合节能率算法定为(spec §5.9.1 选哪个公式):

```
综合节能率 = <填,公式 A 乘法 / 公式 B 加法>
```

**calcComprehensiveRate 新签名**(spec §5.9.3):
```ts
interface ComprehensiveRateInput {
  techs: TechEntry[];
  hospitalType: string;
  mepSystems: string[];
  climateZone?: string;
}
```
```

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/progress/data-flow-audit.md
git commit -m "docs(phase-1): step2 comprehensive rate excel structure confirmation"
```

---

## Task 14: Supabase 现状检查

**Files:**
- Read: `src/shared/lib/supabase.ts`(确认 supabase client 配置)
- Run: SQL 查询(通过 Supabase Studio 或 psql)

- [ ] **Step 1: 确认 Supabase 配置**

Run:
```bash
cat .env.local 2>/dev/null | grep -i supabase | head -5
# 或
grep -rn "supabase" src/shared/lib/ | head -5
```

记录 Supabase URL 和 key 是否配置(不打印 key 值,只说有/无)。

- [ ] **Step 2: 跑 SQL 查询数据量**

打开 Supabase Studio(或用 psql),跑:

```sql
-- 1. 总项目数
SELECT COUNT(*) AS total_projects FROM projects;

-- 2. 有步骤数据的项目数
SELECT COUNT(*) AS projects_with_steps FROM project_steps;

-- 3. 各步骤数据量
SELECT 
  COUNT(*) FILTER (WHERE step3_data IS NOT NULL) AS step3_count,
  COUNT(*) FILTER (WHERE step4_data IS NOT NULL) AS step4_count,
  COUNT(*) FILTER (WHERE step4_data->'decisionData' IS NOT NULL) AS step5_count
FROM project_steps;

-- 4. 验证 decisionData 路径(应返回 'object' 或 NULL)
SELECT 
  jsonb_typeof(step4_data->'decisionData') AS decision_data_type
FROM project_steps LIMIT 5;
```

记录查询结果。

- [ ] **Step 3: 决定迁移策略**

基于 Step 2 结果:
- 如果 `projects_with_steps = 0`(无线上数据): Phase 1.4 不跑迁移 SQL,直接清空重建
- 如果 `projects_with_steps > 0` 且全是开发测试数据: 同上,直接清空
- 如果有真实用户数据: Phase 1.4 完成后跑 spec §8.9 的迁移 SQL,迁移前必须备份

- [ ] **Step 4: 追加到 data-flow-audit.md**

```markdown

## 6. Supabase 现状检查(spec §8.9)

**查询时间**: 2026-07-11(Phase 1.0)

**查询结果**:
- 总项目数: `<填>`
- 有步骤数据的项目数: `<填>`
- Step 3 数据量: `<填>`
- Step 4 数据量: `<填>`
- Step 5(decisionData)数据量: `<填>`
- decisionData 路径验证: `<填,object / NULL / 其他>`

**迁移策略**:
- `<填,无线上数据 -> 直接清空 / 有测试数据 -> 直接清空 / 有真实数据 -> Phase 1.4 跑迁移 SQL + 备份>`

**Phase 1.4 验收条件**(spec §8.9):
- `<填,如 无需迁移,直接清空 / 必须跑迁移 SQL + 备份 + 在 staging 先跑>`
```

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/progress/data-flow-audit.md
git commit -m "docs(phase-1): supabase current state check"
```

---

## Task 15: 更新进度文件,1.0 done

**Files:**
- Modify: `docs/superpowers/progress/phase-1-progress.md`

- [ ] **Step 1: 更新子阶段状态表**

把 `| 1.0 | in_progress | 1.5 天 | - | 前置准备 |` 改成 `| 1.0 | done | 1.5 天 | <实际花的天数> | <遇到的问题摘要> |`。

- [ ] **Step 2: 更新"当前状态"和"下次 Session 接力点"**

把:
```
## 当前状态
- 进行中: 1.0
- 下一步: 1.1

## 下次 Session 接力点
- 当前从 1.0 开始,先建进度文件 + Excel 版本锁 + 探测脚本
```

改成:
```
## 当前状态
- 进行中: (无,1.0 已完成)
- 下一步: 1.1

## 下次 Session 接力点
- 从 1.1 开始,先读 data-flow-audit.md 和 excel-structure-report.md
- 1.1 任务: 技术卡片字段核对
- 1.1 依据: step1-field-diff.md(参考格式)+ excel-structure-report.md 里"节能技术全量信息(技术卡片).xlsx"的结构
- 1.1 产出: tech-card-field-diff.md(参考 step1-field-diff.md 格式)
- 1.1 完成后写 1.2 的详细实施计划
```

- [ ] **Step 3: 补"决策记录"和"遇到的问题"**

在"决策记录"小节追加 1.0 期间的决策(如果有),格式:
```
- 2026-07-11: <决策内容>
  - 理由: <为什么>
  - 备选: <考虑过但没选的>
  - 影响: <影响哪些子阶段>
```

在"遇到的问题"小节记录 1.0 期间遇到的问题(如 Excel 文件读不了、跨表字段不一致等)。

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/progress/phase-1-progress.md
git commit -m "docs(phase-1): mark 1.0 done, handoff to 1.1"
```

---

## Self-Review Checklist

完成所有 Task 后,跑以下检查:

- [ ] **1. Spec coverage**: 对照 spec §5.5/§5.9.6/§8.1/§8.6/§8.7/§8.9,确认每条都有对应 Task:
  - §5.5 数据流盘点 -> Task 9-12 ✓
  - §5.9.6 Step 2 综合节能率 Excel 结构 -> Task 13 ✓
  - §8.1 Excel 结构探测脚本 -> Task 3-4 ✓
  - §8.6 Excel 版本锁 -> Task 2 ✓
  - §8.7 进度文件 -> Task 1 + Task 15 ✓
  - §8.9 Supabase 现状检查 -> Task 14 ✓

- [ ] **2. Placeholder scan**: `grep -rn "<填\|TBD\|TODO" docs/superpowers/progress/` 应该只在模板示例里出现,实际报告里不应有未填的占位符。

- [ ] **3. Type consistency**: Task 9-12 盘点的字段名(`repairCost`/`laborCost`/`maintenanceCategory`/`savingCostRun`/`originalCostRun` 等)要和 spec §5.4/§5.6 一致。

- [ ] **4. 产出物完整**: 
  - `phase-1-progress.md` - 1.0 标 done
  - `excel-version-lock.md` - 30+ 文件 hash
  - `excel-structure-report.md` - 20+ 文件结构
  - `step1-field-diff.md` - 4 类差异分类
  - `data-flow-audit.md` - 6 小节(4 条断点 + Step 2 结构 + Supabase 现状)
  - `scripts/excel-structure-probe.py` - 能跑
  - Step 1 表单字段对齐改动 - lint + build 通过

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-11-phase-1.0-preparation.md`. Two execution options:

**1. Subagent-Driven (recommended)** - 每个 Task dispatch 一个 fresh subagent,Task 之间 review,快速迭代

**2. Inline Execution** - 在当前 session 里逐 Task 执行,有 checkpoint review

**Which approach?**
