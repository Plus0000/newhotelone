# Phase 1.6: Step 2 综合节能率接线 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `calcComprehensiveRate` 从错误的连乘公式重写为权威加法公式（`Σ(组内加和 × 重叠系数 × 能耗权重) × 医院修正`），接通 4 个 1.3 录入的数据文件，让 Step 2 综合节能率弹窗显示真实计算结果。

**Architecture:** 在 `src/steps/step2-solution/constants.ts` 重写 `calcComprehensiveRate`，新签名收 `techs/climateZone/hvacYear/province/hospitalLevel/totalArea` 6 个输入；调用 `overlapCorrection/hospitalCorrection/energyWeight/energyQuota` 4 个查表函数；返回按作用系统分组的明细。新建 `climateZoneMap.ts` 提供省份→气候分区映射。`ComprehensiveRateModal` 从 Step 1 读字段并展示分组明细（UI 布局改造留到 task #44）。

**Tech Stack:** TypeScript + React + Ant Design + Zustand。

**Spec reference:** `docs/superpowers/specs/2026-07-11-knowledge-base-data-import-design.md` §5.9.1-5.9.6（rev 8 已定稿）。

**Authority formula source:** `docs/step2-综合节能率计算逻辑说明.md` 第五节（PM 审核文档）。

---

## 范围说明（重要）

**本 plan 做什么：**
- 重写 `calcComprehensiveRate` 为加法公式
- 接 4 个查表函数（overlapCorrection/hospitalCorrection/energyWeight/energyQuota）
- 新建 `climateZoneMap.ts`（省份→气候分区）
- 改 `ComprehensiveRateModal` 的 `original` 来源（从 energyQuota 算，无数据回退 DEFAULT_ORIGINAL）
- 改调用方传 Step 1 字段
- seed 修正
- 与 PM 文档算例对账（北京三甲 20000㎡ = 31.0%）

**本 plan 不做（留给后续 phase）：**
- ❌ techBoundaries 完整打分（适配度暂时硬编 1.0，留 TODO 给 Phase 1.7）
- ❌ 一票否决项检查（留给 Phase 1.7）
- ❌ ComprehensiveRateModal UI 布局/字段大改（task #44 独立做，本 plan 只做最小改动让 original 跑通）
- ❌ Step 1 新增气候分区字段（用 climateZoneMap 从省份查，不改 Step 1 表单）

**理由：** techBoundaries 打分需要 Step 1 的电价/管路分区/系统自动化等字段，完整接入是独立工作。1.6 聚焦公式接线，先让 31.0% 算例跑通，证明公式正确。1.7 再做适配度细化。

---

## File Structure

| 文件 | 类型 | 职责 |
|---|---|---|
| `src/data/climateZoneMap.ts` | Create | 省份→气候分区映射 + `getClimateZone(province)` |
| `src/steps/step2-solution/constants.ts` | Modify | 重写 `calcComprehensiveRate`（加法公式 + 接 4 个查表函数） |
| `src/steps/step2-solution/components/ComprehensiveRateModal.tsx` | Modify | 从 Step 1 读字段 + original 改用 energyQuota + 展示分组明细（最小改动） |
| `src/steps/step2-solution/index.tsx` | Modify | 传 `climateZone/hvacYear/province/hospitalLevel/totalArea` 给 Modal |

---

## 验证算例（PM 文档第四节）

**医院条件：** 北京（寒冷地区），三级甲等综合医院，10万㎡，2009年建成。

**勾选 5 项技术：**
| 技术 | 作用系统 | 基准节能率 | 适配度（1.6 硬编 1.0） | 修正后节能率 |
|---|---|---|---|---|
| 相变储热供暖 | 供暖系统 | 15% | 100% | 15% |
| IoT+AI 前馈 | 空调制冷系统 | 15% | 100% | 15% |
| 地源热泵 | 空调制冷+供暖 | 40% | 100% | 40% |
| 智能照明控制 | 照明系统 | 60% | 100% | 60%（文档是 51%，1.6 不接适配度故用 60%） |
| 洁净区域升级 | 空调制冷系统 | 35% | 100% | 35% |

**预期计算（1.6 适配度=1.0，与 PM 文档有差异）：**

| 作用系统 | 组内加和 | 技术数 | 重叠系数 | 系统能耗权重（寒冷） | 贡献 |
|---|---|---|---|---|---|
| 供暖系统 | 15% + 40% = 55% | 2 | 0.75 | 22.00% | 55% × 0.75 × 0.22 = 9.075% |
| 空调制冷系统 | 15% + 40% + 35% = 90% | 3 | 0.70 | 22.62% | 90% × 0.70 × 0.2262 = 14.251% |
| 照明系统 | 60% | 1 | 1.0 | 9.36% | 60% × 1.0 × 0.0936 = 5.616% |
| **初步综合节能率** | | | | | **28.94%** |
| 医院修正（2009<2010） | | | | | × 1.1 |
| **最终** | | | | | **31.83%** |

> PM 文档算例是 31.0%（智能照明按 51% 算），1.6 适配度=1.0 故智能照明按 60% 算，得 31.83%。差异在预期内（适配度未接入）。Phase 1.7 接 techBoundaries 后会与文档完全一致。

**验证脚本输出应该 ≈ 31.83%**（容忍 ±0.1% 浮点误差）。

---

## Task 1: 创建 climateZoneMap.ts

**Files:**
- Create: `src/data/climateZoneMap.ts`

**目的：** 提供省份→气候分区映射。Step 1 没有显式气候分区字段，从 `location[0]`（省份）查表得到。

数据来源：GB 50176-2016《民用建筑热工设计规范》附录——中国建筑气候分区。简化版（按省级行政区划归并）：

- **严寒地区**：黑龙江、吉林、辽宁、内蒙古、新疆、青海、西藏
- **寒冷地区**：北京、天津、河北、山西、山东、宁夏、陕西（部分）、甘肃（部分）、辽宁（部分，简化归寒冷）
- **夏热冬冷地区**：上海、江苏、浙江、安徽、江西、河南、湖北、湖南、重庆、四川、贵州、陕西（部分，简化归夏热冬冷）
- **夏热冬暖地区**：福建、广东、广西、海南、台湾、香港、澳门
- **温和地区**：云南、贵州（部分，简化归温和）

> 注：陕西/甘肃/辽宁/四川/贵州等省跨气候区，本表做简化归并。Phase 1.7 可按地市级细化。

- [ ] **Step 1: 创建文件**

```ts
// Source: GB 50176-2016《民用建筑热工设计规范》附录
// 用途: Step 2 综合节能率计算 - 从省份查气候分区
// 说明: 跨气候区省份做简化归并，Phase 1.7 可按地市级细化
// 录入: 2026-07-14

export type ClimateZone =
  | '严寒地区'
  | '寒冷地区'
  | '夏热冬冷地区'
  | '夏热冬暖地区'
  | '温和地区';

const PROVINCE_TO_CLIMATE: Record<string, ClimateZone> = {
  // 严寒地区
  '黑龙江省': '严寒地区',
  '吉林省': '严寒地区',
  '辽宁省': '严寒地区',
  '内蒙古自治区': '严寒地区',
  '新疆维吾尔自治区': '严寒地区',
  '新疆': '严寒地区',
  '青海省': '严寒地区',
  '西藏自治区': '严寒地区',
  '西藏': '严寒地区',

  // 寒冷地区
  '北京市': '寒冷地区',
  '北京': '寒冷地区',
  '天津市': '寒冷地区',
  '天津': '寒冷地区',
  '河北省': '寒冷地区',
  '河北': '寒冷地区',
  '山西省': '寒冷地区',
  '山西': '寒冷地区',
  '山东省': '寒冷地区',
  '山东': '寒冷地区',
  '宁夏回族自治区': '寒冷地区',
  '宁夏': '寒冷地区',
  '甘肃省': '寒冷地区',
  '甘肃': '寒冷地区',

  // 夏热冬冷地区
  '上海市': '夏热冬冷地区',
  '上海': '夏热冬冷地区',
  '江苏省': '夏热冬冷地区',
  '江苏': '夏热冬冷地区',
  '浙江省': '夏热冬冷地区',
  '浙江': '夏热冬冷地区',
  '安徽省': '夏热冬冷地区',
  '安徽': '夏热冬冷地区',
  '江西省': '夏热冬冷地区',
  '江西': '夏热冬冷地区',
  '河南省': '夏热冬冷地区',
  '河南': '夏热冬冷地区',
  '湖北省': '夏热冬冷地区',
  '湖北': '夏热冬冷地区',
  '湖南省': '夏热冬冷地区',
  '湖南': '夏热冬冷地区',
  '重庆市': '夏热冬冷地区',
  '重庆': '夏热冬冷地区',
  '四川省': '夏热冬冷地区',
  '四川': '夏热冬冷地区',
  '陕西省': '夏热冬冷地区',
  '陕西': '夏热冬冷地区',

  // 夏热冬暖地区
  '福建省': '夏热冬暖地区',
  '福建': '夏热冬暖地区',
  '广东省': '夏热冬暖地区',
  '广东': '夏热冬暖地区',
  '广西壮族自治区': '夏热冬暖地区',
  '广西': '夏热冬暖地区',
  '海南省': '夏热冬暖地区',
  '海南': '夏热冬暖地区',
  '台湾省': '夏热冬暖地区',
  '台湾': '夏热冬暖地区',
  '香港特别行政区': '夏热冬暖地区',
  '香港': '夏热冬暖地区',
  '澳门特别行政区': '夏热冬暖地区',
  '澳门': '夏热冬暖地区',

  // 温和地区
  '云南省': '温和地区',
  '云南': '温和地区',
  '贵州省': '温和地区',
  '贵州': '温和地区',
};

/**
 * 从省份名查气候分区。
 * 自动归一化：去掉"省/市/自治区/特别行政区"后缀再查。
 * 未匹配的省份返回 '寒冷地区' 作为安全默认（北京所在气候区，节能率适中）。
 */
export function getClimateZone(province: string): ClimateZone {
  if (!province) return '寒冷地区';

  // 直接查
  if (PROVINCE_TO_CLIMATE[province]) return PROVINCE_TO_CLIMATE[province];

  // 归一化：去后缀
  const normalized = province
    .replace(/(省|市|自治区|特别行政区|维吾尔|壮族|回族)/g, '');

  return PROVINCE_TO_CLIMATE[normalized] ?? '寒冷地区';
}
```

- [ ] **Step 2: 写一个临时验证脚本**

创建 `scripts/verify-climate-zone.ts`（临时文件，验证后删除）：

```ts
import { getClimateZone } from '../src/data/climateZoneMap';

const cases: [string, string][] = [
  ['北京市', '寒冷地区'],
  ['北京', '寒冷地区'],
  ['广东省', '夏热冬暖地区'],
  ['黑龙江', '严寒地区'],
  ['云南省', '温和地区'],
  ['上海市', '夏热冬冷地区'],
  ['内蒙古自治区', '严寒地区'],
  ['未知省份', '寒冷地区'], // 默认值
];

let pass = 0;
let fail = 0;
for (const [input, expected] of cases) {
  const actual = getClimateZone(input);
  if (actual === expected) {
    pass++;
    console.log(`✅ ${input} -> ${actual}`);
  } else {
    fail++;
    console.log(`❌ ${input} -> ${actual} (期望 ${expected})`);
  }
}
console.log(`\n结果: ${pass} 通过, ${fail} 失败`);
process.exit(fail > 0 ? 1 : 0);
```

- [ ] **Step 3: 运行验证脚本**

Run: `npx tsx scripts/verify-climate-zone.ts`
Expected: 8 通过, 0 失败

- [ ] **Step 4: 删除临时脚本**

```bash
rm scripts/verify-climate-zone.ts
```

- [ ] **Step 5: Commit**

```bash
git add src/data/climateZoneMap.ts
git commit -m "feat(data): add province to climate zone map for step2 rate calc"
```

---

## Task 2: 重写 calcComprehensiveRate（新签名 + 加法公式）

**Files:**
- Modify: `src/steps/step2-solution/constants.ts:33-49`

**目的：** 把连乘公式 `1 - Π(1-r_i)` 改为加法公式 `Σ_s[(Σ_{i∈s} adjustedRate_i) × overlap × weight] × hospitalCorrection`，接 4 个查表函数。

适配度暂时硬编 1.0（留 TODO 给 Phase 1.7 接 techBoundaries）。

- [ ] **Step 1: 改 constants.ts**

把 `src/steps/step2-solution/constants.ts` 第 1 行的 import 改为：

```ts
import type { TechEntry } from '@/data/materials';
import { getOverlapCorrection } from '@/data/overlapCorrection';
import { getHospitalCorrection } from '@/data/hospitalCorrection';
import { getEnergyWeight } from '@/data/energyWeight';
```

把第 33-49 行的 `calcComprehensiveRate` 函数**整体替换**为：

```ts
export interface ComprehensiveRateInput {
  techs: TechEntry[];
  climateZone: string;
  hvacYear: number;
  // 以下 3 个字段用于 original 能耗计算（ComprehensiveRateModal 用）
  hospitalLevel: '三级' | '二级';
  province: string;
  totalArea: number;
}

export interface SystemGroupTech {
  techId: string;
  techName: string;
  baseRate: number;
  adaptation: number;
  adjustedRate: number;
}

export interface SystemGroupContribution {
  system: string;
  techs: SystemGroupTech[];
  groupSum: number;
  techCount: number;
  overlapCorrection: number;
  energyWeight: number;
  contribution: number;
}

export interface ComprehensiveRateResult {
  groups: SystemGroupContribution[];
  preliminaryRate: number;
  hospitalCorrection: number;
  finalRate: number;
}

/**
 * 综合节能率计算（加法公式）
 *
 * 公式（来源 docs/step2-综合节能率计算逻辑说明.md 第五节）:
 *   综合节能率 =
 *     Σ_s [ (Σ_{i∈s} 修正后节能率_i) × 重叠修正系数(|s|) × 系统能耗权重(s, 气候区) ]
 *     × 医院整体修正系数(投产年份)
 *
 * 其中:
 *   修正后节能率_i = 基准节能率 × 适配度%
 *   基准节能率 = energySavingRate 区间上限（"5%-15%" 取 15%）
 *   适配度% = 暂时硬编 1.0，TODO Phase 1.7 接 techBoundaries 打分
 *
 * 分组规则:
 *   按 techEntry.affectedSystems 分组。跨系统技术（如地源热泵同时作用于空调制冷+供暖）
 *   在每个作用的系统中都参与计算。
 *
 * @returns null 当 techs 为空
 */
export function calcComprehensiveRate(
  input: ComprehensiveRateInput
): ComprehensiveRateResult | null {
  const { techs, climateZone, hvacYear } = input;

  if (!techs || techs.length === 0) return null;

  // Step 1: 每个技术算 baseRate（区间上限）和 adjustedRate（baseRate × adaptation）
  // affectedSystems 是字符串（如 "空调制冷系统" 或 "空调制冷系统+供暖系统"），需拆分
  const techDataList = techs.map((t) => {
    const range = parseRateRange(t.energySavingRate);
    const baseRate = range ? range.upper : 0;
    const adaptation = 1.0; // TODO Phase 1.7: 接 techBoundaries 打分
    return {
      techId: t.id,
      techName: t.name,
      baseRate,
      adaptation,
      adjustedRate: baseRate * adaptation,
      systems: (t.affectedSystems || '').split(/[+,，、]/).map((s) => s.trim()).filter(Boolean),
    };
  });

  // Step 2: 按作用系统分组
  // 一个技术作用于多个系统时，在每个系统都参与计算
  const systemGroups = new Map<string, typeof techDataList>();
  for (const td of techDataList) {
    for (const sys of td.systems) {
      if (!systemGroups.has(sys)) systemGroups.set(sys, []);
      systemGroups.get(sys)!.push(td);
    }
  }

  // Step 3: 计算每个系统的贡献
  const groups: SystemGroupContribution[] = [];
  for (const [system, techsInGroup] of systemGroups) {
    const techCount = techsInGroup.length;
    const groupSum = techsInGroup.reduce((acc, t) => acc + t.adjustedRate, 0);
    const overlapCorrection = getOverlapCorrection(techCount);
    // 能耗权重只用「全院区综合系统能耗」维度（spec §5.9.1 第 4 条）
    const energyWeight = getEnergyWeight('全院区综合系统能耗', system, climateZone);
    const contribution = groupSum * overlapCorrection * energyWeight;

    groups.push({
      system,
      techs: techsInGroup.map((t) => ({
        techId: t.techId,
        techName: t.techName,
        baseRate: t.baseRate,
        adaptation: t.adaptation,
        adjustedRate: t.adjustedRate,
      })),
      groupSum,
      techCount,
      overlapCorrection,
      energyWeight,
      contribution,
    });
  }

  // Step 4: 初步综合节能率 = Σ(contribution)
  const preliminaryRate = groups.reduce((acc, g) => acc + g.contribution, 0);

  // Step 5: 医院整体修正
  const hospitalCorrection = getHospitalCorrection(hvacYear);
  const finalRate = preliminaryRate * hospitalCorrection;

  return {
    groups,
    preliminaryRate,
    hospitalCorrection,
    finalRate,
  };
}
```

保留 `parseRateRange` 函数不变（第 22-31 行）。

- [ ] **Step 2: 验证 TS 编译**

Run: `npx tsc --noEmit`
Expected: 无错误（旧调用方 `calcComprehensiveRate(selectedTechs)` 会因签名变化报错，下一步修）

- [ ] **Step 3: 不 commit，继续 Task 3**

旧调用方 `ComprehensiveRateModal.tsx:56` 还在用旧签名，下一步一起改完再 commit。

---

## Task 3: 改 ComprehensiveRateModal（original 来源 + 调用方传参）

**Files:**
- Modify: `src/steps/step2-solution/components/ComprehensiveRateModal.tsx`
- Modify: `src/steps/step2-solution/index.tsx`

**目的：**
1. `ComprehensiveRateModal` 从 props 接收 `climateZone/hvacYear/province/hospitalLevel/totalArea`，调 `calcComprehensiveRate` 新签名
2. `original`（耗电量/气/煤/碳）从 `energyQuota` 算（无数据回退 DEFAULT_ORIGINAL）
3. `index.tsx` 从 `useProjectStore` + `step1Data` 读字段传给 Modal

UI 布局大改留到 task #44，本 task 只做最小改动让数据跑通。

- [ ] **Step 1: 改 ComprehensiveRateModal.tsx 的 Props 接口**

把第 15-20 行的 `Props` interface 改为：

```ts
interface Props {
  open: boolean;
  selectedTechs: TechEntry[];
  onClose: () => void;
  onConfirm?: () => void;
  climateZone: string;
  hvacYear: number;
  province: string;
  hospitalLevel: '三级' | '二级';
  totalArea: number;
}
```

- [ ] **Step 2: 改 import**

把第 10 行改为：

```ts
import { CATEGORY_LABELS, calcComprehensiveRate } from '../constants';
import { getEnergyQuota, hasEnergyQuota } from '@/data/energyQuota';
```

- [ ] **Step 3: 改函数签名和 original 初始化**

把第 53-57 行：

```ts
export function ComprehensiveRateModal({ open, selectedTechs, onClose, onConfirm }: Props) {
  const [original, setOriginal] = useState<Record<string, number>>({ ...DEFAULT_ORIGINAL });

  const result = calcComprehensiveRate(selectedTechs);
  const comprehensiveRate = result ? (result.lower + result.upper) / 2 : 0;
```

改为：

```ts
export function ComprehensiveRateModal({
  open,
  selectedTechs,
  onClose,
  onConfirm,
  climateZone,
  hvacYear,
  province,
  hospitalLevel,
  totalArea,
}: Props) {
  const result = calcComprehensiveRate({
    techs: selectedTechs,
    climateZone,
    hvacYear,
    hospitalLevel,
    province,
    totalArea,
  });
  const comprehensiveRate = result ? result.finalRate : 0;
  const hasQuota = hasEnergyQuota(province);

  // original 从 energyQuota 算（无数据省份回退 DEFAULT_ORIGINAL）
  const defaultOriginal = useMemo(() => {
    if (!hasQuota) return { ...DEFAULT_ORIGINAL };
    const electricityPerArea = getEnergyQuota(province, hospitalLevel, '电力', 'comprehensive', 'baseline') ?? 0;
    const gasPerArea = getEnergyQuota(province, hospitalLevel, '天然气', 'comprehensive', 'baseline') ?? 0;
    const heatPerArea = getEnergyQuota(province, hospitalLevel, '市政热力', 'comprehensive', 'baseline') ?? 0;
    // 单位换算:
    //   电力 kWh/(㎡·a) × 面积 / 10000 = 万kWh/年
    //   天然气 Nm³/(㎡·a) × 面积 / 10000 = 万Nm³/年
    //   煤耗和碳排先保留 DEFAULT_ORIGINAL（1.8 接通）
    return {
      electricity: electricityPerArea * totalArea / 10000,
      gas: gasPerArea * totalArea / 10000,
      coal: DEFAULT_ORIGINAL.coal,
      carbon: DEFAULT_ORIGINAL.carbon,
    };
  }, [hasQuota, province, hospitalLevel, totalArea]);

  const [original, setOriginal] = useState<Record<string, number>>(defaultOriginal);

  // 当 defaultOriginal 变化时（如切换项目），同步 state
  useEffect(() => {
    setOriginal(defaultOriginal);
  }, [defaultOriginal]);
```

- [ ] **Step 4: 调整顶部 import 增加 useEffect 和 useMemo**

文件顶部已经有 `import { useState, useMemo } from 'react';`，改为：

```ts
import { useState, useMemo, useEffect } from 'react';
```

- [ ] **Step 5: 修改结果展示文本（适配新返回结构）**

把第 128-134 行：

```tsx
        <div style={{ fontSize: 13, opacity: 0.9, marginBottom: 6 }}>综合节能率估算结果</div>
        <div style={{ fontSize: 32, fontWeight: 700, letterSpacing: 1 }}>
          {result
            ? `${(result.lower * 100).toFixed(1)}% ~ ${(result.upper * 100).toFixed(1)}%`
            : '无法计算'}
        </div>
```

改为：

```tsx
        <div style={{ fontSize: 13, opacity: 0.9, marginBottom: 6 }}>综合节能率估算结果</div>
        <div style={{ fontSize: 32, fontWeight: 700, letterSpacing: 1 }}>
          {result
            ? `${(result.finalRate * 100).toFixed(1)}%`
            : '无法计算'}
        </div>
        {result && (
          <div style={{ fontSize: 12, opacity: 0.85, marginTop: 6 }}>
            初步 {(result.preliminaryRate * 100).toFixed(1)}% × 医院修正 {result.hospitalCorrection} = {(result.finalRate * 100).toFixed(1)}%
          </div>
        )}
```

- [ ] **Step 6: 无数据省份加提示**

在 `{/* Energy comparison cards */}` 注释前（约第 136 行）插入：

```tsx
      {!hasQuota && (
        <div style={{ fontSize: 12, color: '#fa8c16', background: '#fff7e6', padding: '8px 12px', borderRadius: 4, marginBottom: 16 }}>
          该省份无能耗限额数据，原始方案能耗使用默认值。省份: {province}
        </div>
      )}
```

- [ ] **Step 7: 改 index.tsx 传参**

读 `src/steps/step2-solution/index.tsx`，在文件顶部 import 后增加：

```ts
import { getClimateZone } from '@/data/climateZoneMap';
```

在 `Step2Solution` 函数体内（约第 20 行 `const techEntries = useMergedTechEntries();` 后）增加读取 Step 1 字段：

```ts
  const project = useProjectStore((s) => s.project);
  const step1Data = useProjectStore((s) =>
    s.projectId ? s.projectsStep1Data[s.projectId] : undefined
  );

  // 从 project 读省份/等级/面积
  const province = project?.location?.[0] || '';
  const hospitalLevel: '三级' | '二级' =
    project?.hospitalLevel === '三级' ? '三级' : '二级';
  const totalArea = project?.totalArea || 0;

  // 从 step1Data.mep.hvac.coldSourceMeta 取冷源最早投产年份
  const hvacYear = useMemo(() => {
    const coldSourceMeta = (step1Data?.mep as any)?.hvac?.coldSourceMeta;
    if (!coldSourceMeta || typeof coldSourceMeta !== 'object') return new Date().getFullYear();
    const years = Object.values(coldSourceMeta)
      .map((m: any) => Number(m?.year))
      .filter((y) => !isNaN(y) && y > 0);
    return years.length > 0 ? Math.min(...years) : new Date().getFullYear();
  }, [step1Data]);

  const climateZone = getClimateZone(province);
```

把第 224-229 行的 `<ComprehensiveRateModal ... />` 改为：

```tsx
      <ComprehensiveRateModal
        open={rateModalOpen}
        selectedTechs={selectedTechEntries}
        onClose={() => setRateModalOpen(false)}
        onConfirm={handleRateConfirm}
        climateZone={climateZone}
        hvacYear={hvacYear}
        province={province}
        hospitalLevel={hospitalLevel}
        totalArea={totalArea}
      />
```

- [ ] **Step 8: 验证 TS 编译**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 9: 验证 build**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 10: Commit**

```bash
git add src/steps/step2-solution/constants.ts \
        src/steps/step2-solution/components/ComprehensiveRateModal.tsx \
        src/steps/step2-solution/index.tsx
git commit -m "feat(step2): rewrite calcComprehensiveRate with additive formula and wire 4 data files"
```

---

## Task 4: 算例对账验证

**Files:**
- Create: `scripts/verify-step2-rate.ts`（临时验证脚本）

**目的：** 用 PM 文档算例验证公式正确性。脚本构造 5 个 TechEntry，调用 `calcComprehensiveRate`，断言最终结果 ≈ 31.83%。

- [ ] **Step 1: 创建验证脚本**

```ts
import { calcComprehensiveRate } from '../src/steps/step2-solution/constants';
import type { TechEntry } from '../src/data/materials';

// 构造 PM 文档第四节的算例
// 北京三甲 20000㎡，2009 年建成，寒冷地区
// 勾选 5 项技术
const mockTechs: TechEntry[] = [
  {
    id: '1',
    name: '相变储热供暖技术',
    category: '可再生能源利用技术',
    affectedSystems: '供暖系统',
    energyType: '电耗',
    energySavingRate: '5%-15%',
    investmentIndex: '',
    annualEnergy: '',
    paybackPeriod: '',
  } as TechEntry,
  {
    id: '2',
    name: 'IoT+数字孪生+AI前馈调节',
    category: '智能控制及优化技术',
    affectedSystems: '空调制冷系统',
    energyType: '电耗',
    energySavingRate: '10%-15%',
    investmentIndex: '',
    annualEnergy: '',
    paybackPeriod: '',
  } as TechEntry,
  {
    id: '3',
    name: '地源热泵多能源耦合供热',
    category: '可再生能源利用技术',
    affectedSystems: '空调制冷系统+供暖系统',
    energyType: '电耗',
    energySavingRate: '30%-40%',
    investmentIndex: '',
    annualEnergy: '',
    paybackPeriod: '',
  } as TechEntry,
  {
    id: '4',
    name: '智能照明控制技术',
    category: '智能控制及优化技术',
    affectedSystems: '照明系统',
    energyType: '电耗',
    energySavingRate: '40%-60%',
    investmentIndex: '',
    annualEnergy: '',
    paybackPeriod: '',
  } as TechEntry,
  {
    id: '5',
    name: '洁净区域冷热源升级技术',
    category: '能源高效利用技术',
    affectedSystems: '空调制冷系统',
    energyType: '电耗',
    energySavingRate: '25%-35%',
    investmentIndex: '',
    annualEnergy: '',
    paybackPeriod: '',
  } as TechEntry,
];

const result = calcComprehensiveRate({
  techs: mockTechs,
  climateZone: '寒冷地区',
  hvacYear: 2009,
  hospitalLevel: '三级',
  province: '北京',
  totalArea: 100000, // PM 文档算例是 10万㎡（文档标题写 20000㎡ 是笔误，第五节示例条件是 10万㎡）
});

if (!result) {
  console.log('❌ 返回 null');
  process.exit(1);
}

console.log('=== 分组明细 ===');
for (const g of result.groups) {
  console.log(
    `\n[${g.system}] techCount=${g.techCount}, groupSum=${(g.groupSum * 100).toFixed(2)}%, ` +
    `overlap=${g.overlapCorrection}, weight=${(g.energyWeight * 100).toFixed(2)}%, ` +
    `contribution=${(g.contribution * 100).toFixed(2)}%`
  );
  for (const t of g.techs) {
    console.log(`  - ${t.techName}: baseRate=${(t.baseRate * 100).toFixed(0)}%, adaptation=${t.adaptation}, adjusted=${(t.adjustedRate * 100).toFixed(0)}%`);
  }
}

console.log('\n=== 汇总 ===');
console.log(`初步综合节能率: ${(result.preliminaryRate * 100).toFixed(2)}%`);
console.log(`医院修正系数: ${result.hospitalCorrection}`);
console.log(`最终综合节能率: ${(result.finalRate * 100).toFixed(2)}%`);

// 期望: 31.83% (±0.5% 容差)
const expected = 0.3183;
const actual = result.finalRate;
const diff = Math.abs(actual - expected);
const pass = diff < 0.005;

console.log(`\n期望: ${(expected * 100).toFixed(2)}%`);
console.log(`实际: ${(actual * 100).toFixed(2)}%`);
console.log(`差值: ${(diff * 100).toFixed(2)}%`);
console.log(pass ? '✅ 验证通过' : '❌ 验证失败');

process.exit(pass ? 0 : 1);
```

- [ ] **Step 2: 运行验证脚本**

Run: `npx tsx scripts/verify-step2-rate.ts`

Expected 输出：
```
=== 分组明细 ===

[供暖系统] techCount=2, groupSum=55.00%, overlap=0.75, weight=22.00%, contribution=9.08%

[空调制冷系统] techCount=3, groupSum=90.00%, overlap=0.7, weight=22.62%, contribution=14.25%

[照明系统] techCount=1, groupSum=60.00%, overlap=1, weight=9.36%, contribution=5.62%

=== 汇总 ===
初步综合节能率: 28.94%
医院修正系数: 1.1
最终综合节能率: 31.83%

期望: 31.83%
实际: 31.83%
差值: 0.00%
✅ 验证通过
```

如果实际值与期望差 >0.5%，**停下来排查**：检查 `parseRateRange` 是否取区间上限、`getOverlapCorrection` 是否按 techCount 查、`getEnergyWeight` 是否传 `'全院区综合系统能耗'`、`getHospitalCorrection(2009)` 是否返回 1.1。

- [ ] **Step 3: 删除验证脚本**

```bash
rm scripts/verify-step2-rate.ts
```

- [ ] **Step 4: Commit（删除脚本也要 commit）**

```bash
git add -A
git commit -m "chore: remove temporary verification script for step2 rate"
```

---

## Task 5: 浏览器联调验证

**Files:** 无文件改动，纯运行验证

**目的：** 在浏览器跑 dev server，创建一个北京三甲项目，勾选 5 项技术，打开综合节能率弹窗，确认显示 31.8% 左右。

- [ ] **Step 1: 启动 dev server**

Run: `npm run dev`（后台运行）

- [ ] **Step 2: 浏览器手动验证**

打开浏览器访问 dev server URL，执行以下步骤：

1. 登录
2. 创建新项目：
   - 项目名称：`1.6验证`
   - 所在地：北京 / 朝阳区
   - 医院等级：三级
   - 总面积：100000 ㎡
3. 进入 Step 1，填完基本信息 + 机电系统（冷源投产年份填 2009）
4. 进入 Step 2，勾选 5 项技术：相变储热供暖、IoT+AI前馈、地源热泵、智能照明、洁净区域升级
5. 点击「综合节能率估算」按钮
6. 检查弹窗顶部显示的节能率是否在 31.8% 左右（容忍 ±1%）
7. 检查「初步 X% × 医院修正 1.1 = Y%」是否显示
8. 检查原始方案耗电量是否非 250（默认值）—— 北京有数据，应该是 `30 × 100000 / 10000 = 300 万kWh/年`

如果显示错误或 NaN，**停下来排查**：
- 打开浏览器 Console 看报错
- 在 `calcComprehensiveRate` 入口加 `console.log(input)` 确认参数正确
- 检查 `affectedSystems` 字段是否真的包含「+」分隔的多个系统

- [ ] **Step 3: 关闭 dev server**

`Ctrl+C` 停止 dev server。

---

## Task 6: 更新 memory 和 progress 文件

**Files:**
- Modify: `/Users/plus0/.claude/projects/-Users-plus0-newhotelone/memory/knowledge-base-data-import.md`
- Modify: `/Users/plus0/newhotelone/docs/superpowers/progress/phase-1-progress.md`

- [ ] **Step 1: 更新 knowledge-base-data-import.md**

在文件末尾（"## 关联" 之前）插入新章节：

```markdown
## Phase 1.6 完成情况（2026-07-14）

- ✅ 新建 `src/data/climateZoneMap.ts`：省份→气候分区映射（5 分区 × 34 省级行政区）
- ✅ 重写 `calcComprehensiveRate`（`src/steps/step2-solution/constants.ts`）：
  - 旧签名 `(techs: TechEntry[])` -> 新签名 `(input: ComprehensiveRateInput)`
  - 旧公式 `1 - Π(1-r_i)` -> 新公式 `Σ_s[(Σ_{i∈s} adjustedRate) × overlap × weight] × hospitalCorrection`
  - 返回 `ComprehensiveRateResult`（含分组明细）
  - 接 3 个查表函数：overlapCorrection/hospitalCorrection/energyWeight
  - 适配度暂时硬编 1.0（TODO Phase 1.7 接 techBoundaries）
- ✅ 改 `ComprehensiveRateModal.tsx`：
  - Props 增加 `climateZone/hvacYear/province/hospitalLevel/totalArea`
  - `original` 从 `energyQuota` 算（无数据省份回退 DEFAULT_ORIGINAL + UI 提示）
  - 顶部结果区显示「初步 X% × 医院修正 Y = Z%」
- ✅ 改 `index.tsx`：从 `project` + `step1Data.mep.hvac.coldSourceMeta` 读字段传给 Modal
- ✅ 算例验证：北京三甲 10万㎡，2009 年，5 技术 -> 31.83%（与 PM 文档 31.0% 差异在适配度未接入，预期内）
- ⚠️ 待 Phase 1.7：techBoundaries 完整打分（适配度细化）+ 一票否决项检查
```

- [ ] **Step 2: 更新 phase-1-progress.md**

读 `docs/superpowers/progress/phase-1-progress.md`，找到 Phase 1.6 章节，把状态从「待开始」改为「已完成」，加入完成日期和关键产出。

如果文件中没有 Phase 1.6 章节，在 Phase 1.3 完成章节后插入：

```markdown
## Phase 1.6: Step 2 综合节能率接线（2026-07-14 完成）

- 新建 `src/data/climateZoneMap.ts`
- 重写 `calcComprehensiveRate` 为加法公式，接 3 个查表函数
- `ComprehensiveRateModal` original 改用 energyQuota，无数据回退默认值
- 算例验证 31.83%（PM 文档 31.0%，差异为适配度未接入，预期内）
- 适配度打分留 Phase 1.7
```

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/progress/phase-1-progress.md
git commit -m "docs(progress): mark Phase 1.6 complete"
```

memory 文件不 commit（在 ~/.claude 下，不在 repo 内）。

- [ ] **Step 4: 更新任务状态**

调用 TaskUpdate 把 task #43 标记为 completed。

---

## 任务依赖

```
Task 1 (climateZoneMap) ──┐
                          v
Task 2 (重写 calc) ──────> Task 3 (Modal + index) ──> Task 4 (对账) ──> Task 5 (浏览器) ──> Task 6 (memory)
```

Task 1 和 Task 2 可以并行（互不依赖），但 Task 2 必须在 Task 3 之前（Task 3 调用新签名）。

---

## 不做的事

- ❌ techBoundaries 完整打分（适配度=1.0，留 Phase 1.7）
- ❌ 一票否决项检查（留 Phase 1.7）
- ❌ ComprehensiveRateModal UI 大改（task #44 独立做，本 plan 只做最小改动让 original 跑通）
- ❌ Step 1 新增气候分区表单字段（用 climateZoneMap 从省份查）
- ❌ 改 seed 数据（seed 的 location/hospitalLevel/totalArea 字段已存在，无需改）
- ❌ 改 Step 4/5（本 plan 只动 Step 2）

---

## 风险与回退

**风险 1：** `step1Data.mep.hvac.coldSourceMeta` 结构可能和假设不符。
- 缓解：Task 3 Step 7 用 `as any` 兜底，并在 Console 打印实际结构。如果发现结构不符，停下来读 `SubStep4MEP.tsx` 确认。

**风险 2：** `TechEntry.affectedSystems` 字段可能不是「空调制冷系统+供暖系统」格式（可能是数组或其他分隔符）。
- 缓解：Task 4 验证脚本会暴露问题。如果分隔符不是 `+`，改 `calcComprehensiveRate` 中的 split 正则。

**风险 3：** 算例验证失败（实际 ≠ 31.83%）。
- 缓解：Task 4 Step 2 的排查清单。如果排查后仍失败，停下来找用户确认。

**回退：** 如果整体方案有问题，`git revert` 最后 3 个 commit（Task 1/3/6 的 commit），回到 Phase 1.3 完成状态。
